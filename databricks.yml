bundle:
  name: spotify_dab
  uuid: 3231840f-e078-495f-8005-cca66f5e4291

include:
  - resources/*.yml

#artifacts:
 # python_artifact:
  #  type: whl
   # build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405616987721790.10.azuredatabricks.net
      root_path: /Workspace/Users/umutcanasci@posta.mu.edu.tr/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: databricksazureproject
      schema: ${workspace.current_user.short_name}
    presets:
      source_linked_deployment: false
  
  prod:
    mode: production
    workspace:
      host: https://adb-7405616987721790.10.azuredatabricks.net
      root_path: /Workspace/PROD/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: databricksazureproject
      schema: prod
    permissions:
      - user_name: umutcanasci@posta.mu.edu.tr
        level: CAN_MANAGE
